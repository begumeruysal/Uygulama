import 'package:flutter/material.dart';

class OrderScreen extends StatelessWidget {
  const OrderScreen();

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Sipariş Ekranı İçeriği'),
    );
  }
}
