import 'package:flutter/material.dart';

class TrackScreen extends StatelessWidget {
  const TrackScreen();

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Çöp Takip Ekranı İçeriği'),
    );
  }
}
